{
  "name": "charp",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.35",
    "typescript": "^4.6.4"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "yarn build && node ./dist/index.js",
    "vendor": "git submodule update --init --recursive && cd ./vendor/Charp.Core && nuget restore & dotnet build"
  },
  "dependencies": {
    "fs-extra": "^10.1.0",
    "typescript-json-serializer": "^4.2.0"
  }
}
